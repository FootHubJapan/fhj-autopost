# サービスアカウントキーが作成できない場合の代替認証方法

## 問題

組織ポリシー (`iam.disableServiceAccountKeyCreation`) により、サービスアカウントキーの作成が無効化されています。

## 解決策: OAuth 2.0 クライアント認証

既に取得済みのOAuth 2.0クライアントシークレットを使用して認証します。

### 1. OAuthクライアントJSONを配置

```bash
# ダウンロードしたOAuthクライアントシークレットを配置
cp ~/Downloads/client_secret_*.json config/oauth-client.json
```

### 2. 初回認証（一度だけ実行）

```bash
node scripts/auth-oauth.js
```

このコマンドを実行すると：
1. ブラウザで認証URLが開きます
2. Googleアカウントでログインして権限を付与
3. 表示される認証コードをコピー
4. ターミナルに貼り付けてEnter

認証トークンが `config/oauth-token.json` に保存されます。

### 3. 認証後の実行

一度認証すれば、以降は自動的にトークンが使用されます：

```bash
npm run sync
```

## 注意事項

- **トークンの有効期限**: トークンは期限切れになることがあります。その場合は再認証が必要です。
- **セキュリティ**: `config/oauth-token.json` は `.gitignore` に含まれていますが、絶対にGitにコミットしないでください。
- **自動化**: OAuth認証はユーザー認証が必要なため、完全自動化には向いていません。GitHub Actionsで実行する場合は、サービスアカウントキーが必要です。

## 組織ポリシーの無効化（推奨）

可能であれば、組織ポリシー管理者に依頼して、サービスアカウントキー作成の制約を無効化してもらうことをお勧めします。

これにより、より安全で自動化しやすいサービスアカウントキー認証を使用できます。
