# OAuth同意画面への移動方法

## 現在のページ
OAuthの概要ページを開いています。

## 次のステップ

### 方法1: 左側メニューから（推奨）

左側のメニューで **「対象」** をクリックしてください。

これでOAuth同意画面の設定ページに移動し、テストユーザーを追加できます。

### 方法2: 直接URLで開く

以下のURLに直接アクセス：

```
https://console.cloud.google.com/apis/credentials/consent?project=project-7a4b7b1e-1c3a-4c55-b58
```

## OAuth同意画面でテストユーザーを追加

「対象」ページに移動したら：

1. **「テストユーザー」セクションを探す**
   - ページの中央または下部に「テストユーザー」というセクションがあります

2. **「+ ユーザーを追加」または「ADD USERS」ボタンをクリック**

3. **メールアドレスを入力**
   ```
   iyuki09175407@gmail.com
   ```

4. **「追加」または「ADD」をクリック**

5. **変更を保存**
   - ページ上部の「保存」ボタンをクリック（必要に応じて）

## 確認

テストユーザーを追加したら、ターミナルに戻って：

```bash
npm run setup:oauth
```

これで403エラーが解消され、認証が完了するはずです。

