# 認証コードの取得方法

## OAuth認証フローの流れ

1. **認証URLにアクセス**
   - `npm run setup:oauth` で表示されたURLをブラウザで開く

2. **Googleアカウントでログイン**
   - 自分のGoogleアカウント（`iyuki09175407@gmail.com`）でログイン

3. **権限を許可**
   - 「fhj-autopost が次のアクセス権限をリクエストしています」という画面で「許可」をクリック

4. **リダイレクト**
   - 認証後、`http://localhost` にリダイレクトされます
   - **重要**: ローカルサーバーが動いていないので、エラーページが表示される可能性があります

## 認証コードの場所

認証コードは **ブラウザのURLバー** に表示されます。

### リダイレクト後のURL例

```
http://localhost/?code=4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz&scope=https://www.googleapis.com/auth/drive%20https://www.googleapis.com/auth/spreadsheets
```

### 認証コードの部分

URLの中の `code=` の後の文字列が認証コードです：

```
4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz
```

## 手順

1. **URLバーを確認**
   - リダイレクト後、ブラウザのURLバーを見てください
   - `code=` で始まる部分を探してください

2. **認証コードをコピー**
   - `code=` の後の文字列全体をコピー
   - 例: `4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz`

3. **ターミナルに貼り付け**
   - ターミナルに戻り、「認証コードを入力してください:」の後に貼り付けてEnter

## 注意事項

- 認証コードは一度しか使えません
- コードが長い場合、URLバーからコピーする際に全体が表示されないことがあります
- その場合は、URLバーをクリックして全選択（Cmd+A）してからコピーしてください

## エラーページが表示された場合

`http://localhost` にリダイレクトされても、ローカルサーバーが動いていないため「このサイトにアクセスできません」などのエラーページが表示されることがあります。

**問題ありません！** URLバーに認証コードが含まれているので、そこからコピーしてください。
