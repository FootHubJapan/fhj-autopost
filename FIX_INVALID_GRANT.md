# invalid_grant エラーの解決方法

## エラーの原因

`invalid_grant` エラーは、認証コードに関する問題で発生します。

### 考えられる原因

1. **認証コードが既に使用済み**
   - OAuth認証コードは一度しか使えません
   - 同じコードを2回使うとエラーになります

2. **認証コードの有効期限切れ**
   - 認証コードは数分以内に使用する必要があります
   - 時間が経ちすぎると無効になります

3. **認証コードの形式が間違っている**
   - URL全体をコピーしてしまった場合
   - 余分な文字が含まれている場合

## 解決方法

### 1. 認証コードの形式を確認

正しい認証コードは以下のような形式です：
```
4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz
```

**間違った例**:
- `localhost/?code=4/0AeanS...` （URL全体）
- `code=4/0AeanS...` （`code=` が含まれている）
- `4/0AeanS...&scope=...` （`&scope=` 以降が含まれている）

### 2. もう一度認証フローを開始

認証コードは一度しか使えないので、新しい認証コードを取得する必要があります：

```bash
npm run setup:oauth
```

### 3. 認証コードを正しくコピー

1. ブラウザのURLバーで `code=` の後の文字列だけを選択
2. `&scope=` の前までが認証コードです
3. コピーしてターミナルに貼り付け

### 4. すぐに使用する

認証コードを取得したら、すぐに（数分以内に）ターミナルに貼り付けてEnterを押してください。

## 修正内容

コードを修正して、認証コードの前後の不要な文字を自動的に削除するようにしました。

もう一度 `npm run setup:oauth` を実行して、新しい認証コードを取得してください。

