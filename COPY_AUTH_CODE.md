# 認証コードのコピー方法

## ✅ 現在の状態

URLバーに以下のようなURLが表示されています：
```
localhost/?code=4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz&scope=...
```

エラーページが表示されていますが、**これは正常です**。
ローカルサーバーが動いていないため、エラーページが表示されますが、URLバーに認証コードが含まれているので問題ありません。

## 認証コードのコピー手順

### 方法1: URLバーから直接コピー（推奨）

1. **URLバーをクリック**
   - アドレスバー（`localhost/?code=...` が表示されている場所）をクリック

2. **全選択**
   - Mac: `Cmd + A` で全選択
   - Windows: `Ctrl + A` で全選択

3. **コピー**
   - Mac: `Cmd + C` でコピー
   - Windows: `Ctrl + C` でコピー

4. **認証コード部分だけを抽出**
   - コピーしたURLから `code=` の後の部分だけを取り出す
   - 例: `4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz`
   - `&scope=` の前までが認証コードです

### 方法2: 手動で選択してコピー

1. **URLバーをクリック**
2. **`code=` の後の文字列をマウスで選択**
   - `&scope=` の前まで選択
3. **コピー** (`Cmd + C` または `Ctrl + C`)

## ターミナルに貼り付け

1. **ターミナルに戻る**
   - ブラウザからターミナルウィンドウに切り替え

2. **認証コードを貼り付け**
   - 「認証コードを入力してください:」の後に貼り付け
   - Mac: `Cmd + V`
   - Windows: `Ctrl + V`

3. **Enterキーを押す**

## 確認

認証コードは以下のような形式です：
- 例: `4/0AeanS1234567890abcdefghijklmnopqrstuvwxyz`
- `code=` で始まり、`&scope=` の前で終わります

