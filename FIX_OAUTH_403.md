# OAuth 403エラーの解決方法

## 問題

エラー403: アクセス拒否 - OAuthアプリが「テスト中」の状態で、承認されたテスターのみがアクセスできます。

## 解決方法

### 1. Google Cloud ConsoleでOAuth同意画面を開く

1. https://console.cloud.google.com/apis/credentials/consent にアクセス
2. プロジェクト「My First Project」を選択

### 2. テストユーザーを追加

1. 「OAuth同意画面」ページで「テストユーザー」セクションを探す
2. 「+ ユーザーを追加」をクリック
3. 自分のGoogleアカウントのメールアドレスを追加:
   - `iyuki09175407@gmail.com`
4. 「追加」をクリック

### 3. 再度認証を試す

```bash
npm run setup:oauth
```

### 4. 本番モードに変更（オプション）

テストユーザーを追加する代わりに、OAuth同意画面を「本番」モードに変更することもできます：

1. 「OAuth同意画面」ページで「公開ステータス」を「テスト中」から「本番」に変更
2. 必要な情報を入力（プライバシーポリシー、利用規約など）
3. Googleの検証プロセスを完了（必要に応じて）

**注意**: 本番モードにするには、Googleの検証プロセスが必要な場合があります。

## 推奨

まずは「テストユーザー」を追加する方法が最も簡単です。
