# メディア生成の改善計画

## 現状確認

✅ **生成物の場所**: `queue/<platform>/<post_id>/` に存在
- `ig_1080x1350.png` (Instagram画像)
- `tt_1080x1920_cover.png` (TikTokカバー)
- `tt_1080x1920.mp4` (TikTok動画)

✅ **Drive連携**: `meta.json` に `drive` セクションが追加されている

## 問題点

1. **見た目の完成度**: 現在のSVGベースの生成は「それっぽい」がPSDレベルではない
2. **ffmpegの動画**: 単純なズームのみで、テキストの出現タイミングがない

## 改善方針

### 1. テンプレートベースのアプローチ

- `templates/` ディレクトリに背景テンプレートPNGを配置
- カテゴリ×プラットフォームごとにテンプレートを用意
- ffmpegの `drawtext` でテキストを差し込む

### 2. TikTok動画の改善

- 0-2秒: 結論をフェードイン
- 2-8秒: 要点が1つずつ出現（2秒ごと）
- 8-12秒: CTA表示
- zoompanで軽くズーム

### 3. 実装ステップ

1. `templates/` ディレクトリ構造の作成
2. `src/media.js` の改修（テンプレートベースに変更）
3. テンプレート作成ガイドの追加

## 次のアクション

テンプレートベースの実装を開始します。
